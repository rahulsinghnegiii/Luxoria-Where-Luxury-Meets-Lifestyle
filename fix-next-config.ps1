Set-Content -Path "next.config.js" -Value "/** @type {import('next').NextConfig} */`n`nconst nextConfig = {`n  images: {`n    domains: ['images.unsplash.com', 'via.placeholder.com', 'placehold.co'],`n    // Cache images for 24 hours to reduce load`n    minimumCacheTTL: 86400,`n    // Limit the size of images to optimize`n    deviceSizes: [640, 750, 828, 1080, 1200],`n    imageSizes: [16, 32, 48, 64, 96, 128, 256]`n  },`n  // Optimize production build`n  reactStrictMode: true,`n  // Disable source maps in production`n  productionBrowserSourceMaps: false`n}`n`nmodule.exports = nextConfig"
